mixin voteCard(vote = {})
  .voteCard.card
    .voteCard__header.card-header
      img(alt="image")
    .voteCard__body.card-body
      .voteCard__title.card-title=(vote.title)
      .voteCard__dates
        .voteCard__createAt=(vote.createAt)
        .voteCard__dueDate=(`~ ${vote.dueDate}`)
