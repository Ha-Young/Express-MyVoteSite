<!DOCTYPE html>
<html>
  <head>
    <%- include ('partials/head') %>
  </head>
  <body>
    <%- include ('partials/header') %>

    <h1><%= title %></h1>

    <% for (let i = 0; i < votes.length; i++) { %>
      <form action="/votings/<%= votes[i]._id %>" method="GET">
        <div>
          <div>
            <span>투표 주제:  <%= votes[i].title %></span>
          </div>
          <div>
            <span>생성자:  <%= votes[i].creater.nickname %></span>
          </div>
          <div>
            <span>종료 시간:  <%= votes[i].convertedExpiredAt %></span>
          </div>
          <div>
            <span>진행 여부:  <%= votes[i].isProceeding %></span>
          </div>
        </div>
        <button type="submit">투표 하기</button>
        <button type="submit">결과 보기</button>
      </form>
      <br/>
    <% } %>
  </body>
</html>
