<!DOCTYPE html>
<html>

<head>
  <title>home</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <%- include('partials/header') %>

  <% if (user) { %>
  <nav>
    <ul>
      <li><a href='/votings/new'>투표 생성하기</a></li>
      <li><a href='/votings/my-votings'>내 투표 보기</a></li>
    </ul>
  </nav>
  <% } %>

  <main>
    <% if (allVotings) { %>
    <% allVotings.forEach(voting => { %>
    <div class='voting' data-id='<%= voting._id %>'>
      <ul>
        <li>제목: <%= voting.title %></li>
        <li>작성자: <%= voting.author %></li>
        <li>만료 날짜: <%= new Date(voting.endDate) %></li>
        <% if (new Date(voting.endDate).getTime() >= currentDate) { %>
        <li>진행중</li>
        <% } else { %>
        <li>기간 만료</li>
        <% } %>
      </ul>
    </div>
    <% }); %>
    <% } %>
  </main>
  <script src='../javascripts/index.js'></script>
</body>

</html>
