<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
  <a href="/votings/new">Create Vote</a>
  <a href="/my-votings">My Votings</a>
  <ul class="vote-list">
    <% for (let i = 0; i < allVotings.length; i++) { %>
    <% if (allVotings[i].status === "PROCEEDING") { %>
    <% const address = `/votings/${allVotings[i]._id}` %>
    <a class="vote-button" href=<%=address%> <li>
      <h2><%= allVotings[i].title%></h2>
      <h1><%= allVotings[i].creator%></h1>
      </li>
    </a>
    <% } %>
    <% } %>
  </ul>

</body>
<script src="/javascripts/showVote.js"></script>

</html>