<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/stylesheets/votings.css" />
  <title>votings</title>
</head>
<body>
  <% if (isOnVote === "true") { %>
    <div class="onVote-wrapper">
      <form action="/votings/<%= votingId %>" method="POST">
        <div class="title">
          제목: <%= title %>
        </div>
        <div class="creator">
          생성자: <%= creator %>
        </div>
        <div class="endDate">
          만료날짜: <%= endDate %>
        </div>
        <div class="vote-status">
          투표 진행중 ⭕️
        </div>
        <div class="checkBox-wrapper">
          투표목록
          <% for (let i = 0; i < option.length; i++) { %>
            <div>
              <label for="option"><%= option[i].optionTitle %></label>
              <input type="radio" id="option" name="option" value="<%= option[i].optionTitle %>"/>
              <% if (isValidateUser) { %>
                <span>
                  <%= option[i].votedUsers.length %>
                </span>
              <% } %>
            </div>
          <% } %>
        </div>
        <button type="submit">투표하기</button>
        <a href="/">
          <button>투표목록</button>
        </a>
      </form>
    </div>
  <% } else { %>
    <div class="endVote-wrapper">
      <div>end</div>
    </div>
  <% } %>
</body>
</html>
