<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Code-Wars-Problems</title>
    <link href='/stylesheets/style.css' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <nav>
      <ul>
        <% if (user) { %>
        <li><a href='/auth/logout'>Logout</a></li>
        <li><a href='/'>Homepage</a></li>
        <li><a href='/profile'>Profile</a></li>
        <% } else { %>
        <li><a href='/auth/login'>Login</a></li>
        <% } %>
      </ul>
    </nav>
    <% if (user) { %>
    <main>
      <h1>ID: <%= votings[findIndex].id %></h1>
      <h2>Vote Title: <%= votings[findIndex].voteTitle %></h2>

      <div class='votings-list'>
        <ul>
          <li>Expiry Date: <%= votings[findIndex].expiryDate %></li>
          <li>Vote Options1: <%= votings[findIndex].voteOptions1 %></li>
          <li>Vote Options2: <%= votings[findIndex].voteOptions2 %></li>
        </ul>
      </div>

      <form class='form' action='/problems/success' method='post' >
        <div class='answer'>
          <textarea type='text' name='answer_message' placeholder='투표에 대한 상세 내역을 입력하세요!!'></textarea>
          <button class='register-button' type='submit'>제출</button>
        </div>
      </form>
    </main>
    <% } else { %>
    <header>
      <h1>You can use our services after Login.</h1>
      <h2>If you want to use our page Press the Button above.</h2>

      <img src='https://images.unsplash.com/photo-1459347268516-3ed71100e718?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1950&q=80' height='500px' />
    </header>
    <% } %>
  </body>
</html>
