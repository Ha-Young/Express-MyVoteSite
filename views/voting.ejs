<!DOCTYPE html>
<html>
  <head>
      <% include ./partials/head %>
  </head>
  <body>
    <% include ./partials/header %>

    <%if (flashes) { %>
      <p><%= flashes.error %></p>
    <% } %>
    <div style='padding:20px'>​<%= JSON.stringify(voting, null, 2) %></div>
    <p><%= voting.title %></p>
    <p>생성자 <%= voting.author %></p>
    <p>기한 <%= voting.expiration %></p>
    <p class='status'><%= voting.isValid ? '진행중':'종료' %></p>

    <form action='/votings/<%= voting._id %>?_method=PUT' method='POST'>
      <ul class='options'>
        <% for (var i = 0; i < voting.options.length; i++) {%>
          <li>
            <input type='radio' name='option' id='option<%= i %>' value=<%= voting.options[i]._id %>>
            <label for='option<%= i %>'><%= voting.options[i].text %></label>
          </li>
        <% } %>
      </ul>
      <input type='submit' class='submit-button'>
    </form>

</html>
