<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('partials/header') %>

    <form action='/votings/<%= id %>' method='post'>

    <p>제목: <%= vote.title %></p>
    <p>생성자: <%= vote.name %></p>
    <p>만료 날짜 및 시간: <%= vote.expired_at %></p>
    <p>진행 중 여부: <%= vote.status %></p>
    <p>투표 선택 사항</p>

    <ul>
      <% for (var i = 0; i < vote.options.length; i++) { %>
        <li>
          <label>
            <input type='radio' name='voteOptions' value=<%= i %>>
            <%= vote.options[i].text %>
          </label>
        </li>
      <% } %>
    </ul>

    <input type='submit' value='완료'>

    </form>


    <a href='/'>투표 목록보기</a>

  </body>
</html>
