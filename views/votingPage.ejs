<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <div class="votingsPageBox">
    <h1><%= title %></h1>
    <div class="votingDataBox">
      <div class="votingDetails">
        제목: <%= votingData.title %><br>
        작성자: <%= votingData.writer %><br>
        투표 설명: <%= votingData.description %><br>
        만료일: <%= votingData.expiration_date_string %><br>
      </div>
      <% if(votingData.progress) { %>
      <div class="box">
        진행 중
      </div>
      <div class="box">
        <a class="vote votingPageBtn" href="/votings/<%= votingData.id %>">투표하기</a>
      </div>
      <% } else { %>
      진행 종료
      <% } %>
    </div>
    <select>
      <option value="선택">선택</option>
      <% for(let i=0; i < votingData.select_option.length; i++) { %>
      <option class="option" value="<%= votingData.select_option[i] %>"><%= votingData.select_option[i] %></option>
      <% } %>
    </select>
    <% if(deleteBtn) { %>
      <a class="deleteBtn votingPageBtn" href="/votings/<%= votingData.id %>"><%= deleteBtn %></a>
    <% } %>
    <div class="box">
      <% if(!votingData.progress||userCheck) { %>
        <% for(let key in result) { %>
          <div><%= key %> -> <%= result[key]%></div>
        <% } %>
      <% } %>
    </div>
    <div class="box">
      <a class="votingPageBtn" href="/">투표 목록으로 돌아가기</a>
    </div>
  </div>
  <script src="/javascripts/votingPage.js"></script>
</body>

</html>
