<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Document</title>
</head>
<body>
  <header>
    <% include ./partials/header %>
  </header>
  <div class="">
    <h3 class="">New User</h3>
    <form action="/auth/signup" method="POST">
      <div class="">
        <div class="">
          <label for="email" class="">E-mail*</label>
          <div class="">
            <input type="text" name="email" value="<%= userInput.email %>" class="">
            <% if(errors.email){ %>
              <div class="">
                <span class="invalid-feedback"><%= errors.email.message %></span>
              </div>
            <% } %>
          </div>
        </div>
        <div class="">
          <label for="name" class="">Name*</label>
          <div class="">
            <input type="text" name="name" value="<%= userInput.name %>" class="">
            <% if(errors.name){ %>
              <div class="">
                <span class="invalid-feedback"><%= errors.name.message %></span>
              </div>
            <% } %>
          </div>
        </div>
        <div class="">
          <label for="password" class="">Password*</label>
          <div class="">
            <input type="text" name="password" value="<%= userInput.password %>" class="">
            <% if(errors.password){ %>
              <div class="">
                <span class="invalid-feedback"><%= errors.password.message %></span>
              </div>
            <% } %>
          </div>
        </div>
        <div class="">
          <label for="passwordConfirm" class="">Password Confirm*</label>
          <div class="">
            <input type="text" name="passwordConfirm" value="<%= userInput.passwordConfirm %>" class="">
            <% if(errors.passwordConfirm){ %>
              <div class="">
                <span class="invalid-feedback"><%= errors.passwordConfirm.message %></span>
              </div>
            <% } %>
          </div>
        </div>
        <div class="">
          <input type="submit" class="" value="Sign up">
        </div>
      </div>
    </form>
  </div>
</body>
</html>
