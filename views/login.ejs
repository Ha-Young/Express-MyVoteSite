<link rel="stylesheet" href="/stylesheets/style.css" />

<div class="signup-wrapper">
  <div class="login-image-board">
    <span class="login-image-font">LOG IN!</span>
    <span class="login-image-font">LOG IN!</span>
    <span class="login-image-font black">LOG IN!</span>
    <span class="login-image-font">LOG IN!</span>
    <span class="login-image-font ">LOG IN!</span>
    <span class="login-image-font">LOG IN!</span>
  </div>
  <div class="login-board">
    <h1>로그인</h1>
    <form class="login-form" action="/login/auth" method="POST">
      <div class="form-item email">
        <label for="email"></label>
        <span class="form-error-message"><%= messages.filter((message) => message.includes('email') ? true : false )%></span>
        <input name="email" placeholder="email"></input>
      </div>  
      <div class="form-item password">
        <label for="password"></label>
        <span class="form-error-message"><%= messages.filter((message) => message.includes('password') ? true : false )%></span>
        <input name="password" placeholder="password" type="password"></input>
      </div>
      <div class="form-button-board">
        <button type="submit" class="button">GO!</button>
        <button type="button" class="button"><a href="/login/auth/google">Google Login</a></button>
      </div>
    </form>
  </div>
</div>