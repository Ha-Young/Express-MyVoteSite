extends ./layout.pug

block stylesheet
  link(rel='stylesheet', href='/stylesheets/create-result.css')

block content
  - const result = isCreate ? "투표 생성 성공" : "투표 생성 실패"
  section.create-result
    h2 #{result}
    p.countTime
    p 잠시후에 메인페이지로 돌아갑니다.
    a(href="/") 지금 메인페이지로 돌아가기
  script(src="/javascripts/createResult.js")
