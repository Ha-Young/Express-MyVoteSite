<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css" />
    <title>VOTEVOTE</title>
  </head>
  <body>
    <% include ./header %>
    <section class="info-vote">
      <div class="item-vote <%= vote.progress ? '' : 'finished' %>">
        <p class="title"><%= vote.title %></p>
        <p class="creator"><%= vote.creatorName %></p>
        <p class="expiration"><%= vote.expiration_date %></p>
        <p class="progress"><%= vote.progress? "진행" : "완료" %></p>
      </div>
      <div>
        <% if(vote.progress) { %>
        <div class="options">
          <form method="POST" action="/votings/<%= vote._id %>?_method=PUT">
            <ul>
              <% vote.options.forEach(function(option){ %>
              <li>
                <input type="radio" id="<%= option.title %>" value="<%=
                option._id%>" name="option" <%= hasVoted ? 'disabled' : '' %> />
                <label for="<%= option.title %>"><%= option.title %></label>
                <% if(String(user._id) === String(vote.creator)) { %>
                <span><%= option.people.length %> 명</span>
                <% } %>
              </li>
              <% }) %>
            </ul>
            <input type="submit" value="Submit" />
          </form>
        </div>
        <div class="admin">
          <% if(String(user._id) === String(vote.creator)) { %>
          <p>투표 생성자만 투표 진행상황을 확인할 수 있습니다.</p>
          <form method="POST" action="/votings/<%= vote._id %>?_method=DELETE">
            <p><input type="submit" name="delete" value="투표삭제" /></p>
          </form>
          <% } else { %>
          <p>투표가 만료되면 결과확인이 가능합니다.</p>
          <div>
            <% if (hasVoted) { %>
            <p>이미 투표를 하셨습니다. 결과를 기다려주세요.</p>
            <% } %>
          </div>
          <% } %>
        </div>
        <% } else { %>
        <div class="result-vote">
          <div class="result-option">
            <p>최종결과</p>
            <% results.forEach(function(result){ %>
            <span><%= result.title %></span>
            <% }) %>
          </div>
          <ul class="options finished">
            <% vote.options.forEach(function(option){ %>
            <li
              class="<%= option.people.length === maxLength ? 'result' : '' %>"
            >
              <p>
                <%= option.title %>
                <span><%= option.people.length %> 명</span>
              </p>
            </li>
            <% }) %>
          </ul>
        </div>
        <% } %>
      </div>
    </section>
  </body>
</html>
