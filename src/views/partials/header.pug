header.header
  .menu-circle
  .search-bar
    input(type="text" placeholder="Search")
  .header-profile
    .header-profile
      img.profile-img(src="" alt="")
      if (currentUser.id)
        span.current-user Hello #{currentUser.name}
      else
        span.current-user Hello Visitor
